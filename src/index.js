import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'; //import App.js

ReactDOM.render(<App />, document.getElementById('root')); // in here we render our our entire application.
                                                          // every react application starts with a simple div that's named root and you can see the right in the public, index.html
